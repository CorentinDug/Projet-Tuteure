{% extends "v_layout.html.twig" %}
{% block contenu %}
    <form method="post" action="{{ path("supplement.validFormAddSupplement") }} ">
        <div class="row">
            Ajout d'un supplement
            <fieldset>
                <label>Nom du supplement
                    <input name="type_supplement"  type="text"  size="18" value="{{ donnees.type_supplement | default('') }}"/>
                    {% if erreurs.type_supplement is defined %}
                        <small class="error">{{erreurs.type_supplement}}</small>
                    {% endif %}
                </label>
                <input type="submit" name="creerSupplement" value="creer" >
            </fieldset>
        </div>
    </form>
{% endblock %}
